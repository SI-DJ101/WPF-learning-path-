<Window x:Class="Example_16.NewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example_16"
        mc:Ignorable="d"
        Title="Produkt" Height="220" Width="350">

    <Grid x:Name="gridProduct">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Content="Symbol:" Grid.Row="0" Grid.Column="0" Margin="5" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding Symbol}" />

        <Label Content="Nazwa:" Grid.Row="1" Grid.Column="0" Margin="5" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding Name}" />

        <Label Content="Liczba sztuk:" Grid.Row="2" Grid.Column="0" Margin="5"  />
        <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding Quantity}" />

        <Label Content="Magazyn:" Grid.Row="3" Grid.Column="0" Margin="5" />
        <TextBox Grid.Row="3" Grid.Column="1" Margin="5" Text="{Binding StockId}" />

        <Button x:Name="btnConfirm" Content="Potwierdź" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
            HorizontalAlignment="Center" Margin="4" MinWidth="120"
            Click="btnConfirm_Click" />

    </Grid>
</Window>